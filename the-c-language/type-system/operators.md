# Operators

| Arithmetic                                                                                       | Relational                                                                                              | Other                                                                                                |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| <p>Unary plus: <strong>+a</strong><br><code>int x = +5;</code></p>                               | <p>Equality: <strong>a == b</strong><br><code>if (score == 100)</code></p>                              | <p>Ternary: <strong>min = a &#x3C; b ? a : b</strong><br><code>int max = (x > y) ? x : y;</code></p> |
| <p>Unary minus: <strong>-a</strong><br><code>int y = -x;</code></p>                              | <p>Inequality: <strong>a != b</strong><br><code>if (name != "Player")</code></p>                        | <p>Member access: <strong>.</strong><br><code>transform.position</code></p>                          |
| <p>Increment: <strong>++a</strong> or <strong>a++</strong><br><code>score++; ++lives;</code></p> | <p>Less than: <strong>a &#x3C; b</strong><br><code>if (health &#x3C; 50)</code></p>                     | <p>Subscript: <strong>[]</strong><br><code>array[0] = 10;</code></p>                                 |
| <p>Decrement: <strong>--a</strong> or <strong>a--</strong><br><code>ammo--; --energy;</code></p> | <p>Less than or equal: <strong>a &#x3C;= b</strong><br><code>while (i &#x3C;= 10)</code></p>            | <p>Object creation: <strong>new</strong><br><code>GameObject obj = new GameObject();</code></p>      |
| <p>Addition: <strong>a + b</strong><br><code>total = price + tax;</code></p>                     | <p>Greater than: <strong>a > b</strong><br><code>if (speed > maxSpeed)</code></p>                       | <p>Type comparison: <strong>is</strong><br><code>if (obj is Player)</code></p>                       |
| <p>Subtraction: <strong>a - b</strong><br><code>remaining = total - used;</code></p>             | <p>Greater than or equal: <strong>a >= b</strong><br><code>if (level >= 5)</code></p>                   | <p>Query size: <strong>sizeof</strong><br><code>int size = sizeof(int);</code></p>                   |
| <p>Multiplication: <strong>a * b</strong><br><code>area = width * height;</code></p>             | <p>Logical negation: <strong>!a</strong><br><code>if (!isActive)</code></p>                             | <p>Type cast: <strong>as</strong><br><code>Player p = obj as Player;</code></p>                      |
| <p>Division: <strong>a / b</strong><br><code>average = sum / count;</code></p>                   | <p>Logical AND: <strong>a &#x26;&#x26; b</strong><br><code>if (isAlive &#x26;&#x26; hasAmmo)</code></p> |                                                                                                      |
| <p>Modulo (remainder): <strong>a % b</strong><br><code>isEven = (num % 2 == 0);</code></p>       | <p>Logical OR: <strong>a || b</strong><br><code>if (isAdmin || isOwner)</code></p>                      |                                                                                                      |
